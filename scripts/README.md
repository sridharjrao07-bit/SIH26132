# Scripts

Operator helpers. Not imported by the API.

| Script | Purpose |
|---|---|
| `test_db.py` | Live anon-key read of `public.markets` (no secrets printed) |

Demo / judge scripts live in `demo/` (`live_smoke.py`, `mint_admin_token.py`, `reconcile.py`).
